# OpenClaw Monitor 测试计划

## 测试范围

### 1. 单元测试
- [ ] Parser 模块：Cache Trace 解析
- [ ] Database 模块：CRUD 操作
- [ ] Watcher 模块：文件监听
- [ ] WebSocket 模块：连接管理

### 2. 集成测试
- [ ] API 端点测试
- [ ] WebSocket 连接测试
- [ ] 前端界面测试

### 3. 性能测试
- [ ] 大文件解析性能
- [ ] API 响应时间
- [ ] 并发连接测试

### 4. 安全测试
- [ ] API 认证测试
- [ ] 输入验证测试
- [ ] 速率限制测试

---

## 测试用例

### 单元测试

#### TC-001: Cache Trace 解析
**前置条件**：存在有效的 cache-trace.jsonl 文件  
**测试步骤**：
1. 调用 `parseCacheTraceFile()`
2. 验证返回条目数量
3. 验证条目字段正确性

**预期结果**：正确解析所有条目

#### TC-002: Run 转换
**前置条件**：已解析的 cache trace 条目  
**测试步骤**：
1. 调用 `convertToRun()`
2. 验证 Run 对象字段
3. 验证状态计算

**预期结果**：正确转换 Run 对象

#### TC-003: 数据库 CRUD
**前置条件**：数据库已初始化  
**测试步骤**：
1. 创建 Run 记录
2. 读取 Run 记录
3. 更新 Run 记录
4. 删除 Run 记录

**预期结果**：CRUD 操作正常

---

### 集成测试

#### TC-101: API 健康检查
**测试步骤**：
1. GET /health
2. 验证返回状态码 200
3. 验证返回字段完整

#### TC-102: API 获取 Run 列表
**测试步骤**：
1. GET /api/runs
2. 验证返回状态码 200
3. 验证返回数据格式

#### TC-103: API 分页参数验证
**测试步骤**：
1. GET /api/runs?limit=1001
2. 验证返回状态码 400
3. 验证错误信息

#### TC-104: API 速率限制
**测试步骤**：
1. 连续发送 101 次请求
2. 验证第 101 次返回 429

#### TC-105: WebSocket 连接
**测试步骤**：
1. 连接 WebSocket
2. 验证连接成功
3. 验证心跳机制

---

### 性能测试

#### TC-201: 大文件解析
**测试步骤**：
1. 创建 10000 条测试数据
2. 解析文件
3. 测量时间和内存

**预期结果**：
- 解析时间 < 5 秒
- 内存增量 < 100MB

#### TC-202: API 响应时间
**测试步骤**：
1. 发送 100 次 API 请求
2. 测量平均响应时间

**预期结果**：平均响应时间 < 50ms

---

### 安全测试

#### TC-301: API 认证
**测试步骤**：
1. 配置 API_KEY
2. 无 token 访问 API
3. 验证返回 401

#### TC-302: 路径遍历防护
**测试步骤**：
1. POST /api/watcher/reparse
2. body: { filePath: "../../../etc/passwd" }
3. 验证返回 400

---

## 测试环境

- Node.js: 22.22.1
- OS: Linux
- 数据库: SQLite
- 测试框架: Vitest

---

## 测试报告

执行时间：2026-03-17
执行人：AI Assistant
